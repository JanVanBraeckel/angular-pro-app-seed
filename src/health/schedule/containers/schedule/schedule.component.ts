import { Component } from '@angular/core';

@Component({
  selector: 'schedule',
  styleUrls: ['schedule.component.scss'],
  template: `sup boi`
})
export class ScheduleComponent {
  constructor() {}
}
